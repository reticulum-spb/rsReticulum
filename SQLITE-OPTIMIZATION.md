# Оптимизация SQLite для слабого одноплатника

## Цель и область анализа

Повысить пропускную способность и уменьшить задержки обработки пакетов при
высокой нагрузке, сохранив ограниченное потребление памяти.

Анализ выполнен по `logfile` от 15 сентября 2026 года и текущему коду хранилища,
обработчика транспорта, RPC и сохранения маршрутов. Нагрузочный тест намеренно
создаёт тяжёлые условия; уменьшение нагрузки не является предлагаемым решением.
Разделы анализа ниже описывают исходное состояние. Реализованные изменения и
результаты проверок перечислены в разделе «Статус реализации». Результаты
повторного нагрузочного теста приведены в разделе «Нагрузочный тест logfile.next».

**Основной вывод:** главный резерв — объединение записей в короткие транзакции
и уменьшение влияния фоновых операций. Увеличение кэша и очередей не должно быть
первым шагом.

## Что показывает logfile

Лог охватывает примерно 5 минут 45 секунд: 07:32:58–07:38:43 UTC.

| Операция | Предупреждений | Медиана выполнения | Максимум |
| --- | ---: | ---: | ---: |
| `apply` | 95 | 168 мс | 956 мс |
| `clean_known_page` | 57 | 185 мс | 246 мс |
| `collect_packets_page` | 38 | 187 мс | 680 мс |

Это статистика только операций, попавших в предупреждения, а не всех запросов.
Worker пишет WARN при ожидании в очереди или выполнении от 100 мс.

- У 92 из 95 медленных `apply` значение `queue_ms=0`: задержка находится внутри
  выполнения операции, а не перед ней.
- Все 27 предупреждений о чтении `announce` имеют `execution_ms=0` — менее 1 мс
  с учётом округления. При этом они ждут worker от 100 до 601 мс.
- Предупреждений о задержке допуска пакетов — 265; медиана 170 мс, максимум
  893 мс. Максимальный остаток очереди в этих сообщениях — 28 при лимите 64.
- Сообщений о переполнении очереди и ошибках хранилища в этом логе нет.
- `preparation delayed` часто повторяет длительность `apply`: несколько WARN
  могут отражать последствия одной задержки, их времена нельзя просто складывать.
- Шесть завершённых sweep занимают 254–935 мс.
- Самый долгий завершённый GC занимает 102 секунды и удаляет 2130 announce и
  2231 пакет. Это время всего прохода, включая паузы и ожидание основного трафика,
  а не непрерывная блокировка SQLite.

**Интерпретация:** быстрые чтения ждут записей и фоновых операций. Однако
`execution_ms` измеряет прошедшее время и включает возможное вытеснение потока
при загрузке CPU. Лог не разделяет SQL, commit/fsync и автоматический checkpoint.
Стоимость синхронизации накопителя — вероятная причина, но пока не доказанная
отдельным измерением.

## Текущая архитектура

### Worker и обработка announce

- Одно соединение SQLite принадлежит отдельному потоку `rns-storage`.
- До восьми незавершённых запросов; слот удерживается до получения или удаления
  результата. Это ограничивает и накопление готовых ответов.
- Не зависящие от SQLite сообщения могут обрабатываться во время работы БД.
- Для зависимых сообщений существует очередь до 64 элементов / 256 КиБ.
- Кэш метаданных ограничен 256 записями / расчётным бюджетом 256 КиБ.
- Основной путь обычно накапливает запись одного принятого announce.

Последовательность основного пути:

```text
Чтение нужного состояния → обработка announce → накопление записи
→ commit этой записи → подготовка следующего зависимого сообщения
```

В `prepare_inner` сначала выполняется `write(...).await`, затем чтения. Интерфейс
поддерживает пакетные мутации, но несколько входящих announce обычно не объединяются
в одну транзакцию. Увеличение `MAX_BATCH_ITEMS` само по себе этого не изменит.

### SQLite и схема

- WAL, `synchronous=FULL` по умолчанию.
- Основной кэш по умолчанию 1024 КиБ; временный — 256 КиБ.
- `mmap_size=0`, `temp_store=FILE`.
- `wal_autocheckpoint=128` страниц: около 512 КиБ при странице 4 КиБ.
- Кэш подготовленных SQL-запросов — 16 элементов.
- Метаданные announce и его пакет записываются атомарно.
- Хеши, ссылки и ограничения внешних ключей защищают целостность данных.
- Таблицы маршрутов пока сохраняются отдельными снимками MessagePack.

В WAL режим `FULL` синхронизирует WAL при каждом commit. Автоматический checkpoint
выполняется потоком, чей commit пересёк порог, поэтому его стоимость может попадать
в длительность `apply`. См. [SQLite synchronous](https://www.sqlite.org/pragma.html#pragma_synchronous)
и [WAL performance](https://sqlite.org/wal.html#performance_considerations).

### Sweep и GC

Примерно раз в минуту sweep собирает пакеты, нужные текущим и сохранённым маршрутам.
Временная таблица `sweep_seen` содержит наблюдённый набор; `packet_keep` сохраняет
защиту пакетов между проходами и перезапусками. Незавершённый sweep не должен
снимать прежнюю защиту.

GC обходит данные по ключам без `OFFSET`, по 16 кандидатов в транзакции. После
порции предусмотрена пауза втрое длиннее её выполнения. Новая порция запускается
при отсутствии основной работы в проверяемых очередях. Уже начатая операция
не прерывается появлением пакета.

Индексы основных проверок ссылок уже присутствуют. `EXPLAIN QUERY PLAN` на
воспроизведённой схеме подтвердил использование ключей и индексов в запросах GC.
Это структурная проверка на локальном SQLite 3.31.1, не план и не замер на базе
целевого устройства. `FinishSweep` просматривает весь `packet_keep` и удаляет
отсутствующие в новом наборе записи одной транзакцией.

## Предлагаемые изменения

### 1. Ограниченное объединение announce в транзакции

Основной кандидат на ускорение с сохранением `FULL`.

Стартовые параметры для эксперимента, не готовые гарантии производительности:

- до 16–32 announce в транзакции;
- до 32–64 КиБ на буфер с учётом выделенной памяти, включая ёмкость контейнеров;
- отправка через 5–10 мс после первой записи или раньше при заполнении;
- одна транзакция выполняется, максимум один следующий буфер накапливается;
- при исчерпании бюджета зависимая обработка останавливается.

Интервал ограничивает время накопления, а не длительность commit. Два буфера
потребуют порядка 64–128 КиБ в рамках выбранного бюджета, без неограниченной
очереди отложенных записей. Общую память очереди и буферов нужно учитывать вместе.

Необходимые условия корректности:

- последующие сообщения видят ещё не записанные изменения;
- RPC сохраняют порядок, подтверждения сохранения выдаются после commit;
- GC не удаляет пакеты, нужные незавершённым изменениям;
- снимки маршрутов ссылаются на уже зафиксированные пакеты;
- завершение процесса дожидается принятых записей;
- ошибки commit явно обрабатываются без незаметной потери данных.

При объединении повторных изменений одного назначения нельзя терять старые
пакеты, ещё нужные маршрутам. Простое накопление в worker недостаточно: требуется
изменить текущую последовательную подготовку зависимых сообщений в actor.

### 2. Подготовленные SQL-запросы и повторная валидация

Основные записи внутри `apply`, а также `put_packet` и `require_packet` не используют
`prepare_cached`, хотя кэш уже настроен. Перевести повторяющиеся INSERT/UPDATE/SELECT
на подготовленные запросы в рамках ограниченного кэша.

Проверка заголовка и хеша пакета повторяется при отправке запроса, выполнении
запроса и внутри `put_packet`. Возможное решение — внутренний тип проверенного
запроса с повторным использованием результата проверки. Валидация публичных
точек входа и ограничения целостности должны сохраниться.

Ожидаемый эффект — меньше CPU и временных выделений памяти. Нельзя заранее
приписывать этим затратам наблюдаемые сотни миллисекунд.

### 3. Настройка checkpoint

Сравнить пороги 128, 512 и 1024 страницы при неизменном основном кэше 1 МиБ.
Больший порог может уменьшить частоту checkpoint, но увеличивает WAL и потенциальную
длительность отдельного прохода.

Увеличение WAL не требует такого же увеличения кэша SQLite, но влияет на файловый
кэш ОС и должно сопровождаться измерением памяти системы. Порог checkpoint
не является жёстким ограничением размера WAL.

Следующий вариант — отдельно планировать checkpoint по размеру WAL и состоянию
очереди. Нельзя рассчитывать только на простой: при постоянной нагрузке WAL
должен продолжать обслуживаться. Само перенесение checkpoint в отдельную задачу
на том же worker не устраняет задержку уже начавшейся операции.

### 4. Уменьшение влияния GC

Лимит 16 кандидатов уже есть, но он не ограничивает время синхронизации транзакции.
Простое уменьшение порции может увеличить число commit и общую стоимость уборки.

Варианты для измерения:

- адаптивный размер порции с учётом отдельно измеренных SQL и commit;
- небольшой строго ограниченный объём удаления ненужных пакетов внутри очередной
  транзакции записи, с повторной проверкой всех ссылок;
- порционный `FinishSweep`, сохраняющий защиту старых и новых пакетов при
  прерывании обхода.

Совмещение уборки с записью позволяет разделить стоимость commit, но может
ухудшить задержку этой записи. Решение принимать по p95/p99, не только по скорости GC.

### 5. Избирательная инвалидация кэша

После прямых RPC код безусловно устанавливает `result.clear=true`, включая
читающий `GetRecentAnnounces`. Кэш также очищается после sweep и порций GC,
удаливших метаданные.

Предлагается:

- не сбрасывать кэш после читающих RPC;
- обновлять или удалять только затронутые назначения после мутаций;
- инвалидировать только удалённые назначения после GC;
- поддерживать счётчик байтов кэша инкрементально вместо полного суммирования
  в каждом `trim_sqlite_cache`.

Это уменьшает повторные чтения и работу CPU без увеличения кэша. Обновление
кэша должно учитывать подготовленные сообщения и порядок завершения операций.

## Память: места, не ограниченные размером SQL-страницы

Текущие ограничения очередей и кэшей не являются верхней границей памяти всего
хранилища или процесса.

| Место | Текущее поведение | Направление оптимизации |
| --- | --- | --- |
| Sweep | Полный `HashSet` хешей, затем сортированный `Vec` | Передавать ограниченные порции во временную таблицу |
| Чтение снимков | Файл читается и декодируется целиком | Последовательное чтение записей |
| Сохранение маршрутов | Клонирование таблиц для снимка | Сократить временные копии, сохранив согласованный снимок |
| `GetRecentAnnounces` | Все страницы собираются в массив и сортируются | Постраничный RPC и адаптация потребителей |
| Некоторые выборки identity | Накопление результатов всех страниц | Обработка порциями без общего массива |

При переработке sweep необходимо продолжать защищать пакеты текущих маршрутов
и последнего сохранённого снимка, включая ситуацию неудачного сохранения.
Нельзя заменять полный набор ограниченным буфером, теряя эту гарантию.

## Отдельный режим: synchronous=NORMAL

`SqliteOptions` уже поддерживает `durable_commits=false`, но runtime сейчас не
читает этот параметр из конфигурации и использует значение по умолчанию.

`NORMAL` в WAL может уменьшить число синхронизаций без роста прикладных буферов.
Цена — возможность потери последних подтверждённых транзакций после отключения
питания или сбоя ОС. Это затрагивает и пользовательские закрепления, а также
требует проверки согласованности SQLite с отдельными снимками маршрутов.
См. [гарантии synchronous](https://www.sqlite.org/pragma.html#pragma_synchronous).

Основной план — пакетная запись с `FULL`. `NORMAL` следует рассматривать как
отдельный явно выбираемый режим с описанными гарантиями сохранности.

## План реализации и проверки

1. **Добавить агрегированные измерения.** Число всех транзакций, мутаций и байтов;
   время SQL отдельно от commit; события и длительность checkpoint; попадания
   кэша; максимумы очередей и буферов. Использовать фиксированные счётчики и
   гистограммы, без накопления событий и подробного лога каждого пакета.
2. **Выполнить небольшие локальные оптимизации.** Подготовленные запросы,
   избирательная инвалидация кэша, устранение повторной работы после проверки
   границ валидации.
3. **Внедрить ограниченное объединение announce.** Проверить порядок RPC,
   видимость незаписанных изменений, ошибки записи, GC и завершение процесса.
4. **Повторить исходный нагрузочный тест.** Сравнить варианты checkpoint,
   сохраняя одинаковые входные данные, нагрузку и бюджет памяти.
5. **Снизить пики памяти.** Переработать sweep, чтение снимков и большие RPC.
6. **Оптимизировать GC по измерениям.** Контролировать задержки основного трафика
   и способность очистки справляться с постоянным поступлением данных.

Критерии сравнения:

- пропускная способность и число обработанных announce;
- p50/p95/p99 задержки обработки пакетов по всем событиям, не только WARN;
- потери пакетов и переполнения очередей;
- число commit на announce, время commit и checkpoint;
- максимальный RSS, прикладные буферы и давление на память системы;
- рост WAL, базы и задолженности GC при длительной нагрузке;
- корректность после штатного завершения, аварийного завершения и ошибок записи.

Количество WARN само по себе не является критерием успеха. Увеличение очередей,
пула соединений или кэша не входит в начальный план: сначала нужно сократить
работу на пакет и число транзакций при фиксированном бюджете памяти.

## Основные участки кода

- [Worker](crates/rns-transport/src/storage/worker.rs)
- [Запросы, мутации, лимиты и валидация](crates/rns-transport/src/storage/mod.rs)
- [SQLite backend](crates/rns-transport/src/storage/sqlite.rs)
- [Схема](crates/rns-transport/src/storage/schema.sql)
- [Подготовка сообщений, кэш, RPC, sweep и GC](crates/rns-transport/src/actor/sqlite.rs)
- [Обработка announce](crates/rns-transport/src/actor/inbound.rs)
- [Снимки состояния](crates/rns-transport/src/actor/persistence.rs)
- [Чтение и запись MessagePack](crates/rns-transport/src/persistence.rs)
- [Настройка SQLite в runtime](crates/rns-runtime/src/reticulum.rs)
- [Исходный лог](logfile)


## Статус реализации — 15 сентября 2026

### Реализовано

- Фиксированные гистограммы времени ожидания и выполнения всех операций worker,
  включая быстрые операции; сводки раз в минуту при работе и при завершении.
  Для транзакций `apply` и фоновых проходов отдельно измеряются работа до commit
  и commit, число элементов и объём буфера. Автоматический checkpoint включён
  во время commit: это не отдельное измерение `fsync`. Ошибки до commit учитываются
  в статистике worker, но не в статистике попыток commit.
- Сводки допуска: попадания/промахи кэша точечных чтений, задержки очереди,
  максимальные размеры очереди и накопленных записей. Перцентили из гистограмм
  являются верхними границами бакетов (`p95_upper_ms` и аналогичные поля),
  а не точными перцентилями сохранённых событий.
- Подготовленные SQL-запросы в основном пути записи, без увеличения кэша страниц.
- Читающие RPC и sweep не сбрасывают кэш метаданных. Точечные RPC инвалидируют
  затронутые назначения; GC возвращает список реально удалённых назначений.
  Незавершённые изменения защищены от такой инвалидации и вытеснения.
- Пакетная запись обычных announce: до 32 элементов / целевых 64 КиБ,
  накопление до 10 мс. RPC, чтения пакетов и приоритетный трафик проходят через
  барьер фиксации предыдущих записей. Во время commit зависимая обработка ждёт:
  второго заполняемого буфера нет. Таймер не ограничивает время исполнения SQL.
  Одиночное крупное значение обрабатывается отдельно в рамках прежнего общего
  лимита запроса 256 КиБ; байтовый порог не служит причиной потери такого пакета.
- Новый `TransportQuery::GetAnnouncesPage { after, limit }` возвращает
  `Announces`, упорядоченные по хешу назначения: до 128 записей / 256 КиБ.
  Следующий курсор — последний `dest_hash`, конец — пустая страница.
  Работает с SQLite и memory backend; неверный лимит возвращает ошибку RPC,
  не останавливая хранилище. Общего снимка между страницами нет.
- `rncp`, `rnsh` и `probe` получают публичный ключ через точечный `Recall`.
- Проверка существования identity читает одну запись вместо всех её назначений.
  Для `BlackholeIdentity` полный обход сохранён: он нужен для удаления всех
  соответствующих маршрутов.
- Снимки для sweep читаются последовательно, включая пути внутри большого
  туннеля. В worker передаются порции до 128 хешей. Ошибка чтения, неизвестная
  версия или ошибка записи прерывают sweep без снятия старой защиты пакетов.
- Набор текущих маршрутов представлен одним компактным вектором хешей,
  сортируется и освобождается до чтения снимков с диска. Полного дополнительного
  `HashSet` и декодированных таблиц с диска больше нет.
- Настройка `storage.wal_checkpoint_pages`, диапазон 64–4096, default 128.
  Режим `FULL`, кэш 1 МиБ, ограничения очередей и политика пауз GC сохранены.

### Локальное сравнение пакетной записи

Сценарий: release-сборка, 32 заранее подготовленных валидных announce, реальный
SQLite в режиме `FULL`, дополнительная задержка 50 мс на каждый `Apply` для
моделирования медленной записи. Время измерено до ответа RPC, видящего все
32 зафиксированных announce; подготовка пакетов и завершение процесса не входят.
Три повтора на вариант, без потерь.

База сравнения — коммит `e00b8f6`, до внедрения пакетной записи. Скрипт и отдельные
копии исходников созданы в `/tmp`, в Git не включаются.

| Вариант | Время трёх повторов | Транзакций на 32 announce | Максимальный буфер мутаций |
| --- | --- | ---: | ---: |
| До пакетирования, checkpoint 128 | 3162 / 3278 / 3077 мс | 32 | 1095 байт |
| После пакетирования, checkpoint 128 | 122 / 130 / 163 мс | 1 | 12768 байт |
| После пакетирования, checkpoint 512 | 122 / 114 / 155 мс | 1 | 12768 байт |
| После пакетирования, checkpoint 1024 | 113 / 163 / 113 мс | 1 | 12768 байт |

В этом сценарии медиана уменьшилась примерно в 24 раза за счёт сокращения числа
транзакций. Это не прогноз ускорения реальной сети на одноплатнике. Короткая
нагрузка недостаточна для выбора порога checkpoint: уверенного различия между
порогами нет, default остаётся 128.

Максимальный RSS процесса тестового стенда для порогов 512 и 1024 составил
9192 и 9368 КиБ соответственно. Это память всего локального стенда, не лимит
демона и не сравнение RSS до/после. Ограниченность буфера подтверждается отдельно
счётчиками и тестами.

### Проверки и ограничения

- Проверены пакетная фиксация и порядок RPC, отправка одиночного announce по
  таймеру, запрет снимка после ошибки записи, инвалидация кэша, GC под нагрузкой,
  приоритеты трафика и завершение обработки.
- Проверяются точные списки удалённых GC назначений, постраничный обход без
  пропусков, байтовый лимит страницы, чтение array/map MessagePack и отказ при
  обрыве снимка с сохранением старых и новых pins.
- Настройки checkpoint проверяются при неизменном кэше страниц.
- Итоговый transport-прогон: 509 успешных тестов, 5 отключённых; две подтверждённые
  исходные ошибки фильтрации исключены явно. Тесты runtime: probe — 7, rncp — 8,
  rnsh — 9; все успешны. `cargo check --workspace --features rns-runtime/sqlite-bundled`
  и `cargo check -p rns-transport --no-default-features` успешны.
- В полном release-прогоне на промежуточной версии: 506 успешных тестов,
  5 отключённых, 2 ошибки. Обе ошибки воспроизведены отдельно на `e00b8f6`:
  `plain_group_filter_checks_wire_hops_with_context_and_client_exemptions` и
  `tunnel_synthesis_requires_exact_payload_length`. Они относятся к исходному
  поведению фильтрации пакетов, а не к SQLite.
- Четыре необязательных Python reference-теста (два в transport, два в runtime)
  раньше мешали самой компиляции из-за отсутствующих скриптов. Скрипты теперь читаются только при явном запуске этих
  отключённых тестов; отсутствующие скрипты не подменялись заглушками.

### Что остаётся для следующего этапа

- Повтор исходной длительной сетевой нагрузки на одноплатнике с новыми сводками,
  измерением RSS, давления на память, WAL и задолженности GC.
- Решение о размере порции GC, совмещении уборки с записью и порционном
  `FinishSweep` по времени SQL/commit на целевом накопителе.
- Сокращение клонирования таблиц при сохранении маршрутов. Компактный вектор
  текущих хешей для согласованного sweep всё ещё занимает O(числа маршрутов).
- Старый полный `GetRecentAnnounces` сохраняет прежний контракт и может выделять
  большой ответ; потребители перечислений должны переходить на новый API страниц.
- Повторная хеш-валидация и полный подсчёт байтов небольшого кэша пока сохранены.
- Режим `NORMAL` не включён: гарантии сохранности при отключении питания не ослаблены.


Новые участки реализации:

- [Фиксированные метрики](crates/rns-transport/src/storage/metrics.rs)
- [Последовательное чтение снимков](crates/rns-transport/src/storage/snapshot.rs)
- [Постраничный RPC](crates/rns-transport/src/messages.rs)
- [Настройка checkpoint](CONFIG.md#storage)


### Логирование памяти процесса

В секции `logging` параметр `rss_interval` задаёт интервал в секундах:
по умолчанию 300, `0` отключает измерения. В Linux/Android владеющий runtime
(не подключённый клиент) пишет на уровне INFO строку `process memory` с
`rss_kib` и доступным `peak_rss_kib`. Первый замер — через заданный интервал;
изменение конфига требует перезапуска. Сводки SQLite имеют отдельный интервал.

Читается `/proc/self/status` в блокирующем пуле с фиксированным буфером 16 КиБ,
история не хранится. RSS относится ко всему процессу, пик — ко времени с его
запуска; это приблизительные счётчики [ядра Linux](https://docs.kernel.org/filesystems/proc.html).


## Нагрузочный тест logfile.next

Лог от 15 сентября 2026 года: 08:44:21–09:15:32 UTC, **31 минута 11 секунд**.
Зафиксированы запуск и получение сигнала остановки. Сравнение с исходным
`logfile` (5 минут 45 секунд) ниже нормировано на длительность; одинаковость
входного потока, состояния базы и внешней нагрузки этими логами не подтверждается.
Исходный лог не содержит новых агрегированных метрик.

### Результат

Ошибок ERROR, `failed=true`, ненулевых счётчиков ошибок операций/commit и сообщений
о переполнении или отбрасывании не обнаружено. Это отсутствие зарегистрированных
ошибок, а не доказательство доставки каждого сетевого пакета.

Главная оставшаяся задержка находится в **commit, включая автоматический
checkpoint**, а не в SQL до commit. Объединение записей в текущем потоке даёт
небольшой эффект; увеличивать кэш или очереди по результатам этого теста оснований нет.

| Метрика по опубликованным сводкам | Значение |
| --- | ---: |
| SQL до commit для `apply`: среднее / максимум | 0,048 / 0,297 мс |
| Commit для `apply`: среднее / максимум | 74,166 / 1032,542 мс |
| Доля commit во времени SQL + commit для `apply` | 99,94% |
| Транзакции `apply` / операции в них | 2778 / 3010 |
| Операций на транзакцию: среднее / максимум | 1,084 / 11 |
| Выполнение чтения `announce`: среднее / максимум | 0,030 / 25,319 мс |
| Ожидание допуска: среднее / максимум | 12,985 / 1162,968 мс |
| Попадания / промахи кэша | 22858 / 3021 (88,33% попаданий) |
| Максимум элементов очереди допуска | 52 из 64 |
| Максимум учитываемых байтов очереди | 77158 байт (75,35 КиБ) |
| Максимум учитываемого буфера записей | 9789 байт (9,56 КиБ) |

В 26 из 30 сводок commit `apply` верхняя граница медианы — 64 мс;
в 23 из 30 верхняя граница p95 — 256 мс. Медиана ожидания допуска во всех
32 сводках не выше 1 мс. Это интервальные оценки по гистограммам, а не точные
процентили за весь тест; усреднять p95 отдельных интервалов нельзя.

**Ограничение полноты:** последние сводки worker и транзакций опубликованы
соответственно около 09:14:32 и 09:14:43 (`apply`), до остановки в 09:15:31.
Финальная сводка допуска есть, финальных сводок worker/транзакций нет.
Поэтому суммы выше относятся к опубликованным интервалам, не ко всем операциям
до завершения процесса. Различие числа транзакций и worker-операций объяснимо
несовпадающими границами отчётов и само по себе не свидетельствует об ошибке.

### Сравнение предупреждений

| Предупреждение | Исходный лог, в минуту | Новый лог, в минуту |
| --- | ---: | ---: |
| Медленный `apply` | 16,50 | 16,39 |
| Задержка допуска | 46,03 | 34,38 |
| Медленный `clean_known_page` | 9,90 | 0,45 |
| Медленный `collect_packets_page` | 6,60 | 1,25 |
| Задержка чтения `announce` | 4,69 | 0,38 |

Частота предупреждений допуска снизилась примерно на 25%, но медленные записи
остались практически на прежнем уровне. Максимумы нового теста выше:
`apply` 1032 против 956 мс, допуск 1162 против 893 мс (значения WARN округлены вниз).
Более длинный тест имеет больше возможностей поймать редкие выбросы.
Улучшение GC также нельзя целиком приписывать коду: объём удаляемых данных различен.
WARN допуска, подготовки и worker могут описывать одну цепочку ожидания;
складывать их длительности как независимые задержки нельзя.

### Всплеск нагрузки и фоновое обслуживание

Около 08:59:58–09:00:35 UTC наблюдались commit `apply` до 1,033 с,
`collect_packets_page` до 1,047 с и ожидание допуска до 1,163 с.
В пятиминутной группе сводок на 15–20-й минутах очередь достигла 52 элементов;
в следующих группах максимумы составили 16, 13 и 16. По этим сводкам постоянного
нарастания очереди нет, однако до лимита в пике оставалось всего 12 элементов.

Зафиксирован 31 завершённый sweep: медиана длительности 579 мс, максимум 3056 мс.
Зафиксирован 31 завершённый GC: медиана 2105 мс, максимум 25155 мс;
удалено 542 announce и 1425 пакетов. В исходном тесте удалено 2130 announce
и 2403 пакета. Длительность GC включает паузы и ожидание трафика, это не
непрерывная блокировка worker.

Sweep выполнил по опубликованным сводкам 527 транзакций `keep_packets`, обработав
63908 хешей; commit этих транзакций суммарно занял 13,675 с. Страницы
`collect_packets_page` потратили на commit 25,410 с против 206,032 с у `apply`.
Фоновые операции добавляют задержки, но основной бюджет здесь приходится на запись
прикладных изменений. Счётчик `items` для GC означает просмотренные ключи, не удаления.

### Следующие шаги

1. Измерить причины отправки пакета записей: таймер 10 мс, лимиты, чтение/RPC,
   приоритетный пакет и обслуживание. Среднее 1,084 операции на транзакцию показывает,
   что синтетический выигрыш от пакетов по 32 записи на этом трафике не воспроизводится.
   Лог не позволяет определить, что именно чаще мешает объединению.
2. После этого сравнить окна накопления 10/25/50 мс на сопоставимом потоке,
   сохранив лимиты 32 операции / 64 КиБ и барьеры консистентности. Сравнивать
   транзакции в секунду, ожидание допуска и очередь; большее окно само добавляет
   ожидание одиночным записям и не гарантирует выигрыш.
3. Отдельно сравнить `wal_checkpoint_pages` 128/512/1024. Текущий таймер commit
   включает checkpoint, синхронизацию и возможное вытеснение потока; по нему
   нельзя утверждать, что все 74 мс — чистое время fsync. Не менять одновременно
   окно накопления и checkpoint, иначе причина результата останется неясной.
4. Проверить завершение worker и выгрузку последних метрик при остановке:
   отсутствие финального отчёта мешает полной сверке счётчиков. Оно само по себе
   не доказывает потерю записей.
5. В следующем запуске использовать уже добавленный `logging.rss_interval: 300`.
   В этом логе RSS отсутствует; размеры двух ограниченных буферов не измеряют
   память процесса и не исключают утечки. Длительный поиск утечек остаётся отложенным.

Рабочий код по результатам этого анализа не менялся. Логи и временные средства
анализа не включены в git.


### Диагностика причин отправки пакетов записей

После анализа `logfile.next` добавлена строка `SQLite batch flush summary`
в существующий минутный отчёт actor и его отчёт при остановке. Для неё достаточно
уровня DEBUG у `rns_transport::actor::sqlite`.

- `batches`, `items`, `singletons`: число отправленных пакетов записей, операций
  в них и пакетов из одной операции. Это отправки накопленных announce из actor,
  а не все SQL-транзакции: отдельные изменения из RPC здесь не учитываются.
  Счётчики отражают попытки отправки, успешность проверяется по метрикам worker.
- `timer`: истёк обычный срок накопления 10 мс.
- `path_response`: срок был ускорен из-за ответа на поиск пути.
- `rpc`: следующим в очереди стоит RPC, требующий commit предыдущих записей.
- `priority`: следующим стоит приоритетное сообщение, кроме RPC.
- `packet_read`: следующее сообщение не является обычным announce и требует
  барьера; сюда относятся запросы пакетов и другие зависимые сообщения.
- `item_limit`, `byte_limit`: текущий пакет достиг 32 операций или 64 КиБ.
- `next_capacity`: следующий обычный announce нельзя добавить из-за лимита
  операций или консервативной оценки размера будущего пакета.
- `background`: в момент отправки выполняется sweep, GC или обслуживание.
- `shutdown`: actor завершает работу.

**Условия учитываются совместно:** таймер, фоновая операция и RPC могут совпасть
при одной отправке. Поэтому сумма счётчиков причин может превышать `batches`.
`timer` и `path_response` различают обычный и ускоренный срок; это не два счётчика
одного и того же таймера. Между сводками все счётчики сбрасываются.

Используется фиксированный набор целочисленных счётчиков и один флаг на actor;
история пакетов не хранится, отдельных строк на каждую запись нет. Окно 10 мс,
лимиты пакета и очереди, режим FULL и checkpoint остаются прежними.

Следующий сопоставимый нагрузочный запуск должен показать, преобладает ли
`timer` при высокой доле `singletons` или отправку вынуждают барьеры/обслуживание.
Только после этого выбирать изменение окна; увеличение окна при преобладании
барьеров может почти не уменьшить число транзакций.


Проверка диагностики: 19 тестов `actor::sqlite::tests` прошли последовательно.
При параллельном запуске 4 теста превысили таймауты (drain IFAC, drain всех классов,
control при flood и остановка при GC backlog); в последовательном прогоне все
четыре прошли без изменения таймаутов.


## Нагрузочный тест logfile.batch и настройка окна

`logfile.batch`: 15 сентября 2026, 09:34:32–09:50:18 UTC, около 15 минут 46 секунд.
По 16 сводкам actor (включая остановку):

| Показатель | Значение |
| --- | ---: |
| Пакеты / операции | 1392 / 1529 |
| Отправки с истёкшим обычным таймером | 1351 (97,05%) |
| Пакеты из одной операции | 1282 (92,10%) |
| Среднее операций на пакет | 1,098 |
| Приоритетные сообщения / фоновые операции | 28 / 14 |
| RPC, чтения, ответы на поиск пути, лимиты | 0 |
| Максимум очереди / учитываемых байтов очереди | 31 / 44944 |
| Максимум учитываемого буфера записей | 8929 байт |

Условия отправки могут пересекаться. В опубликованных сводках транзакций commit
`apply` занимает в среднем 67,137 мс (максимум 602,095 мс), SQL до commit —
0,050 мс (максимум 0,267 мс). Среднее ожидание допуска — 8,564 мс,
максимум — 666,210 мс. Зарегистрированных ошибок операций/commit и ERROR нет.
Как и раньше, финальные сводки worker/транзакций отсутствуют, поэтому их суммы
не охватывают весь запуск.

**Вывод:** отправку почти всегда определяет окно 10 мс, а не барьеры или лимиты.
Однако 1529 накопленных операций за этот запуск — всего около 1,6 записи/с;
высокая общая нагрузка не означает плотный поток новых записей announce.
Увеличение окна до 25–50 мс может дать умеренный эффект, но не гарантирует
пакеты по десятки операций. Усреднённый поток не описывает его всплески.

RSS впервые доступен: на 5/10/15-й минутах 18932 / 19820 / 20852 КиБ
(18,49 / 19,36 / 20,36 МиБ). Рост за десять минут — 1920 КиБ; трёх точек
недостаточно, чтобы отличить заполнение рабочих таблиц/кэшей и удержание памяти
аллокатором от утечки. Пиковый RSS совпадает с текущим во всех трёх замерах.

### Следующий контролируемый эксперимент

Добавлен параметр `storage.announce_batch_delay_ms` (1..=10000 мс), по умолчанию
10 мс. Значение проходит из YAML через runtime в actor; при старте выводится
`SQLite announce batching configured delay_ms=...`. Срок отсчитывается от первой
записи и не продлевается поступлением следующих. Ответы на поиск пути и остальные
барьеры по-прежнему могут вызвать более раннюю отправку.

Для следующего сопоставимого запуска после сборки новой версии:

```yaml
storage:
  announce_batch_delay_ms: 50

logging:
  filter: "info,rns_transport::actor::sqlite=debug,rns_transport::storage::metrics=debug"
  rss_interval: 300
```

Это изменение только окна накопления; checkpoint оставить прежним. Более длинное
окно добавляет ожидание фиксации одиночной записи. При заполненной очереди или
занятом worker фактическая задержка может быть больше настроенного окна.
Лимиты памяти не увеличены: прежние 32 операции / 64 КиБ, с существующим исключением
для одной большой записи. После пересборки следующие значения можно менять
конфигом с перезапуском, без новой сборки.

Сравнивать `items/batches`, `singletons/batches`, отправки в секунду при сопоставимом
потоке операций, задержки допуска и очередь. `wait_before_preparation` не измеряет
время от создания накопленной записи до её commit, поэтому по этой метрике одной
нельзя оценивать полную задержку долговечности. Если эффект 50 мс мал, обсуждать
более длинное окно с учётом допустимой задержки, а не увеличивать его вслепую.


### Расширение окна до 10 секунд

Диапазон `storage.announce_batch_delay_ms` расширен до 1..=10000 мс,
по умолчанию по-прежнему 10 мс. Для ближайшего теста выбран пример:

```yaml
storage:
  announce_batch_delay_ms: 2000
```

При редком трафике можно проверить 5000–10000 мс: больше записей потенциально
попадёт в одну транзакцию, что может уменьшить число commit и синхронизаций
накопителя. Фактическое уменьшение записи на SD нужно измерять отдельно;
фоновые транзакции и барьеры остаются. Равномерные записи раз в 1,1 секунды
помещаются в окно 2 секунды, но ранний барьер может отправить пакет раньше.
Окно отсчитывается от первой записи, не продлевается следующими и не увеличивает
лимиты буфера. При выключении питания ещё не зафиксированные записи могут
потеряться; FULL обеспечивает долговечность после успешного commit.


## Полный тест logfile.batch_2000

Лог от 15 сентября 2026: 10:01:24–10:31:18 UTC, **29 минут 54 секунды**.
На старте подтверждено `delay_ms=2000`, лимиты 32 операции / 65536 байт.
В отличие от предыдущих запусков, в этом файле есть финальные отчёты worker
и накопленных транзакций после сигнала остановки. Счётчики согласованы:
1195 отправленных actor пакетов, 1195 `apply` у worker и 1195 транзакций
с 2791 операцией. Это сверка счётчиков, а не проверка содержимого базы.

### Эффект объединения

Сравнение полных запусков с `logfile.batch` (окно 10 мс, 15 минут 46 секунд):

| Метрика | 10 мс | 2000 мс |
| --- | ---: | ---: |
| Операций накопленных announce в минуту | 96,98 | 93,35 |
| Отправленных пакетов в минуту | 88,29 | 39,97 |
| Операций на пакет | 1,098 | 2,336 |
| Пакетов из одной операции | 92,10% | 41,92% |
| Отправок с истёкшим обычным таймером | 97,05% | 10,29% |
| Отправок при наличии следующего приоритетного сообщения | 2,01% | 85,77% |
| Средний commit `apply` | 67,14 мс | 168,59 мс |
| Среднее ожидание допуска | 8,56 мс | 35,31 мс |
| Максимальное ожидание допуска | 666,21 мс | 1824,92 мс |
| Максимум очереди | 31 | 53 из 64 |
| Максимум учитываемого буфера записей | 8929 байт | 10090 байт |

Частота отправок снизилась на 54,7% при потоке операций ниже всего на 3,7%.
При нормировании на одну операцию требуется примерно на 53% меньше пакетов.
Это устойчивый эффект batching; фактическое уменьшение байтов записи на SD
и износа этими счётчиками не измеряется. Полный выигрыш по задержкам не получен:
commit и ожидание допуска в среднем стали дольше. Сравнение не является строгим
A/B: длительности, состояния баз и внешняя нагрузка различаются; у старого запуска
нет последней части отчётов worker/транзакций.

Всего 501 одиночный пакет; таймер отмечен при 123 отправках, приоритетное
сообщение — при 1025, фоновые операции — при 54. RPC, чтения, ответы на поиск
пути внутри накопленного пакета и ограничения ёмкости не отмечены.
Счётчики условий могут пересекаться. Рост `priority` не означает рост самого
приоритетного трафика: при длинном окне он чаще приходит, пока записи ещё накоплены.

### Замедление прошло

Группы по пять последовательных сводок commit `apply` (приблизительно по пять
минут; интервалы worker немного смещены относительно actor):

| От начала запуска | Средний commit |
| --- | ---: |
| 0–5 минут | 84,45 мс |
| 5–10 минут | 367,71 мс |
| 10–15 минут | 280,70 мс |
| 15–20 минут | 83,10 мс |
| 20–25 минут | 81,01 мс |
| 25–конец | 91,03 мс |

Пик commit — 1449,108 мс. SQL до commit остался быстрым: среднее 0,086 мс,
максимум 0,384 мс. Замедление затронуло и фоновые commit, затем прошло без смены
окна. Лог не разделяет checkpoint, синхронизацию накопителя и вытеснение потока,
поэтому его причину установить нельзя. Увеличение окна само по себе не объясняет
временной профиль этих задержек.

Максимумы очереди по пяти последовательным сводкам actor: 16, 48, 53, 16, 21, 18.
Постоянного нарастания очереди нет; максимум учитываемого объёма — 78334 байта
(76,50 КиБ). Зарегистрированных ошибок операций/commit, ERROR и сообщений о
переполнении/отбрасывании не обнаружено. Завершены 29 sweep и 27 GC; максимум
длительности GC 94,915 с включает ожидание и уступки основному трафику.

### Память

| Минута | RSS, КиБ | Пиковый RSS, КиБ |
| --- | ---: | ---: |
| 5 | 19676 | 19676 |
| 10 | 20892 | 20892 |
| 15 | 21688 | 21944 |
| 20 | 22976 | 23268 |
| 25 | 22956 | 24292 |

RSS к последнему замеру — 22,42 МиБ, пик — 23,72 МиБ. Между 20-й и 25-й минутами
RSS практически стабилен; прирост от первого до последнего замера — 3280 КиБ.
Это не доказательство отсутствия утечки, но и не непрерывный рост во всех точках.
30-й замер не ожидается: процесс остановлен чуть раньше 30 минут.

### Следующий шаг

Оставить окно 2000 мс как измеренный вариант для дальнейшей работы. Увеличение
до 10000 мс на этом потоке, вероятно, даст ограниченный дополнительный эффект:
большинство отправок уже вызывает приоритетный барьер.

Следующая точка анализа — разделить `priority` на запросы пути, ответы пути и
подтверждения LRProof; затем проверить зависимости от накопленных записей.
Сейчас наличие следующего приоритетного сообщения вызывает commit всего пакета.
Ослаблять этот барьер без проверки зависимостей и тестов консистентности нельзя.
Отдельный эксперимент с checkpoint проводить при неизменном окне; для объяснения
временного замедления нужны также измерения I/O и нагрузки планировщика.

Рабочий код по этому результату не менялся; исходные логи в git не включены.


## Приоритетные барьеры: уточнение и первая оптимизация

После полного теста с окном 2000 мс проверены `sqlite_reads`, подготовка сообщений
и обработчик запросов пути. Для запроса неизвестного пути нет SQL-чтения:
обработчик запрашивает announce у локального получателя либо пересылает запрос
в другие интерфейсы. Фиксация посторонних announce перед таким запросом не нужна.

Добавлено узкое исключение из приоритетного барьера. Без commit проходят только
обычные DATA/PLAIN запросы пути с контекстом NONE и достаточным payload, если:

- для запрашиваемого destination нет записи в таблице маршрутов;
- среди накопленных `PutAnnounce` нет этого destination.

Запросы известных путей (в том числе с просроченной записью маршрута), запросы
к накопленным announce, ответы пути, LRProof, RPC и чтения пакетов сохраняют
прежний барьер. Запрос неизвестного пути всё равно может совпасть с истёкшим
таймером, лимитом, фоновым обслуживанием или остановкой — эти причины commit
не отключаются. Неизвестный путь не означает отсутствие announce в самой БД:
существенно, что обработчик этого запроса при отсутствии маршрута его не читает.

В `SQLite batch flush summary` добавлены:

| Поле | Значение |
| --- | --- |
| `priority_path_request` | Commit перед запросом пути, сохранившим барьер |
| `priority_path_response` | Commit перед входящим ответом пути |
| `priority_lrproof` | Commit перед подтверждением LRProof |
| `priority_other` | Остальные приоритетные сообщения |
| `unknown_path_bypasses` | Запросы неизвестных путей, обработанные при накопленных записях без отправки пакета |

Первые четыре счётчика в сумме равны прежнему `priority`. `priority_path_response`
отличается от `path_response`: первый относится к следующему входящему сообщению,
второй — к ускоренному сроку фиксации уже принятого ответа пути. Счётчик bypass
не входит в число отправок и не учитывает случаи, когда сработал другой повод
commit. Сводка с bypass печатается даже при отсутствии отправок в интервале.
Дополнительная постоянная память — пять счётчиков u64 на actor; истории нет.

Регрессионный тест проверяет фактическую пересылку неизвестного запроса при
накопленном постороннем announce без вызова `Apply`, включая инъекцию ошибки
commit. Затем запрос к накопленному destination обязан вызвать `Apply`;
последующий RPC видит сохранённую запись либо ошибку хранилища.

Для следующего сопоставимого теста оставить `announce_batch_delay_ms: 2000`
и прежний checkpoint. Ожидаемый эффект проверять по `unknown_path_bypasses`,
частоте пакетов на единицу входящих записей и задержкам допуска. Остальные
категории `priority_*` покажут, где требуется дальнейший анализ зависимостей.


## Результат logfile.batch_2: обход неизвестных путей

Лог от 15 сентября 2026: 10:42:19–11:40:43 UTC, **58 минут 24 секунды**.
На старте подтверждено окно 2000 мс. В конце есть сигнал остановки, но нет
финальных сводок actor/worker или сообщения о сохранённом hashlist. Поэтому
ниже приведены суммы опубликованных интервалов, а не полная сверка до закрытия
хранилища. Сводки actor и транзакций заканчиваются в разное время.

### Сравнение с предыдущим запуском

Для сопоставимости взяты первые 30 сводок каждого типа нового запуска и
30 сводок предыдущего `logfile.batch_2000` (29 минут 54 секунды). Интервалы
транзакций и actor немного смещены; это не строго синхронный A/B-тест.

| Метрика | До обхода неизвестных путей | После, первые 30 сводок |
| --- | ---: | ---: |
| Записей в минуту по actor | 93,36 | 97,39 |
| Отправок пакетов в минуту | 39,98 | 25,95 |
| Операций на пакет | 2,336 | 3,752 |
| Одиночных пакетов | 41,92% | 17,07% |
| Среднее ожидание допуска | 35,31 мс | 5,58 мс |
| Максимальное ожидание допуска | 1824,92 мс | 495,39 мс |
| Средний commit `apply` | 168,59 мс | 109,25 мс |
| Максимум очереди | 53 | 33 |

Отправок в минуту стало примерно на 35% меньше при потоке записей на 4% выше;
на одну операцию требуется примерно на 38% меньше пакетов. Улучшение объединения
подтверждается счётчиками; весь выигрыш задержек нельзя приписывать только коду,
поскольку в прошлом запуске был отдельный длительный период медленных commit.

### Весь запуск

По 58 сводкам actor: 1493 пакета, 5312 операций, **3,558 операции на пакет**,
288 одиночных пакетов (19,29%). Частота — около 25,73 отправки и 91,54 операции
в минуту за охваченный сводками период.

`unknown_path_bypasses=4099`: запросы неизвестных путей действительно проходили
при накопленных записях без их отправки. Это число обработанных запросов,
не точное число сэкономленных транзакций: несколько запросов могли приходиться
на один пакет.

Оставшиеся причины отправок: `timer=1048` (70,19% пакетов), `priority=349`
(23,38%), `background=94`, `path_response=5`. Внутри `priority`:
317 запросов известных/зависимых путей, 28 ответов пути, 4 LRProof, прочих нет.
Лимиты количества и байтов не срабатывали. Условия отправки могут пересекаться.

По транзакционным сводкам: 1501 `apply`, 5332 операции, максимум 23 операции
в транзакции. Эти числа больше actor-счётчиков из-за несовпадающих концов
интервалов и потенциальных отдельных RPC-записей; по этому логу нельзя считать
разность ошибкой учёта или потерей данных.

Средний commit — 105,84 мс, максимум 500,84 мс; SQL до commit — 0,119 мс
в среднем, максимум 0,494 мс. По группам из десяти сводок средний commit:
114,16 / 113,32 / 100,78 / 105,23 / 100,60 / 99,29 мс. Предыдущего длительного
скачка к 300–400 мс нет. Среднее ожидание допуска за весь опубликованный период —
6,15 мс, максимум 918,85 мс. Максимум очереди — 47 из 64, учитываемых байтов
очереди — 65990, буфера записей — 12747 (12,45 КиБ).
Ошибок операций/commit, ERROR и сообщений о переполнении/отбрасывании не найдено.

### Фоновая работа и память

Завершены 57 sweep (максимум 2,028 с) и 42 GC. Самый долгий GC — 337,268 с:
удалено 1767 announce и 1924 пакета; поздние проходы достигали 121–128 с при
удалении примерно 220 пакетов. Это длительность прохода с паузами и уступками
трафику, не непрерывная блокировка SQLite. Все завершённые GC суммарно удалили
1767 announce и 4115 пакетов.

Время commit опубликованных `apply` — 158,87 с. Фоновые операции суммарно
потратили на commit около 161,77 с: удаление пакетов 93,89 с, `keep_packets`
46,31 с, очистка announce 14,81 с, начало/конец sweep 6,76 с. Теперь фоновая
работа сопоставима с прикладными записями по сумме измеренного времени commit.
Количество GC-транзакций не равно количеству синхронизаций накопителя:
часть страниц ничего не меняет.

RSS по пятиминутным замерам, КиБ:
19660 → 21140 → 21072 → 22028 → 22724 → 23372 → 24636 → 24836 → 25636 → 25956 → 25740.
Последний RSS — 25,14 МиБ, максимальный зафиксированный пиковый RSS — 27,92 МиБ.
От первого до последнего замера рост 6080 КиБ за 50 минут; последний замер ниже
предыдущего. Это не доказывает утечку, но и не позволяет утверждать, что память
уже вышла на постоянный уровень. Небольшой ограниченный буфер записей не объясняет
всю память процесса; длинный поиск утечек остаётся отдельной задачей.

### Вывод и дальнейший приоритет

Обход неизвестных путей оставить: он уменьшил число пакетов записи при
сопоставимом входном потоке и сохранил барьеры для зависимых сообщений.
Окно пока оставить 2000 мс. Таймер снова стал основной причиной отправок,
но перед увеличением окна до 5–10 секунд следует проверить доступность worker
для GC: проходы уже растягиваются на минуты, а время ожидания commit не измеряет
время ожидания накопленной записи до её отправки.

Следующий полезный анализ — планирование GC/sweep и число реально изменяющих
данные фоновых транзакций. Обход известных путей требует отдельной проверки
зависимости от сохранённого packet blob и метаданных; снимать этот барьер
по одному имени сообщения нельзя. Влияние на объём записи и износ SD
по-прежнему требует измерения I/O, а не только счётчиков commit.

Рабочий код в ходе анализа этого лога не менялся. Логи в git не включены.


## Диагностика фоновых транзакций и длительности GC

Проверка планировщика показала две причины длительных проходов: страница GC
запускается только при отсутствии foreground job, накопленных записей и входных
очередей; после каждой страницы дополнительно назначается пауза в три её
измеренных длительности. Окно 2000 мс и основной трафик могут надолго откладывать
очередную страницу. Старое `duration_ms` не различало обработку страниц и ожидание.

Уже существующий код `KeepPackets` вставляет в постоянную таблицу только новые
защищённые хеши; неизменный keep-set не переписывается. Поэтому число вызовов commit
не равно числу записывающих транзакций. Удаляющие страницы также могут не удалить
ни одной строки. Убирать сам вызов commit не является обоснованной оптимизацией:
он ещё завершает транзакцию и, для keep-set, изменения временной таблицы.

Добавлены поля в существующую `SQLite transaction summary`:

- `affected_main_rows`: строки основной БД, затронутые DML до commit;
- `main_write_transactions`: транзакции с ненулевым таким счётчиком;
- `no_main_write_transactions`: остальные транзакции, включая изменения только TEMP;
- `main_write_commit_us`, `no_main_write_commit_us`: суммарное время commit
  соответствующей группы, включая возможный автоматический checkpoint.

Для GC/Apply используется разность SQLite `total_changes` (включая побочные
изменения строк). Для sweep отдельно считаются изменения постоянных таблиц;
записи/очистка `temp.sweep_seen` исключены. SQLite может учитывать UPDATE, даже
когда новое значение равно старому. Эти показатели не измеряют физические байты
записи, число fsync или износ SD. При ошибке commit затронутые до него строки
не означают долговечное сохранение — рядом остаётся `commit_failures`.

В `SQLite garbage collection completed` добавлены `pages` и `page_time_ms`.
Последнее — сумма времени от запуска задания страницы до получения результата
actor, включая очередь worker и задержку обработки результата. Разность с
`duration_ms` показывает время вне этих заданий: паузы, foreground и другие
фоновые операции. Это не чистый SQL-таймер; для SQL/commit есть отдельные метрики.
Счётчики сбрасываются при начале следующего прохода, а не при каждом sweep,
если GC ещё не закончил предыдущий проход.

История запросов не хранится. Дополнительные метрики состоят из фиксированных
числовых полей. Порядок транзакций, размер GC-страницы, коэффициент паузы,
окно накопления и ограничения памяти буферов не менялись. Следующий замер с
окном 2000 мс должен показать, что выгоднее: уменьшать записывающие транзакции,
ускорять сканирование пустых страниц или менять доступ GC к worker.

Добавлен тест на реальной SQLite: TEMP-вставки и повторное сохранение существующих
pins не считаются записью основной БД, новый pin считается один раз, снятие
защиты и удаление действительно ненужных пакетов учитываются, пустой GC — нет.


## Результат logfile.gc

Лог от 15 сентября 2026: 11:53:22–12:22:33 UTC, **29 минут 11 секунд**,
окно 2000 мс. Есть финальные сводки actor и сохранение hashlist, но нет финальных
сводок worker/транзакций; их суммы относятся к опубликованным интервалам.

### Что действительно требует commit с изменениями

| Операция | Транзакций с затронутыми строками main | Без затронутых строк main | Commit первой группы | Commit второй группы |
| --- | ---: | ---: | ---: | ---: |
| `apply` | 723 | 0 | 81,071 с | 0 |
| `clean_known_page` | 273 | 1285 | 29,341 с | 2,990 мс |
| `collect_packets_page` | 763 | 963 | 51,254 с | 2,550 мс |
| `keep_packets` | 226 | 215 | 13,023 с | 3,821 мс |
| `begin_sweep` | 29 | 0 | 2,306 с | 0 |
| `finish_sweep` | 29 | 0 | 1,688 с | 0 |

**Вывод:** пустые GC-транзакции не являются существенной стоимостью commit:
2248 таких транзакций суммарно заняли всего 5,54 мс в commit. Это не измерение
времени всего сканирования, но оснований оптимизировать их как дорогие fsync нет.
Фоновые транзакции с затронутыми строками потратили около 97,61 с против
81,07 с у `apply`; собственно удаления — 80,59 с.

Средний commit записывающей страницы: очистка announce 107,48 мс,
удаление пакетов 67,17 мс. По опубликованным интервалам удаление пакетов затронуло
3969 строк в 763 записывающих транзакциях (около 5,2 строки на такую транзакцию),
очистка announce — 2809 строк в 273 (около 10,3). Эти средние не описывают
распределение удаляемых ключей и не гарантируют выигрыш при увеличении страницы.

### Почему GC занимает минуты

Завершены 16 проходов, обработано 3516 страниц. Сумма длительности проходов —
1026,585 с, суммарное время заданий страниц — 81,273 с. **Около 92,1% времени
прошло вне заданий страниц**, то есть в паузах, ожидании foreground/накопленных
записей и выполнении других фоновых задач.

Самый долгий проход: 566,129 с, 453 страницы, из них задания страниц 45,645 с;
удалено 2718 announce и 3075 пакетов. Другой проход: 219,497 с, задания страниц
14,566 с. Медиана всех проходов — 7,985 с: большой исходный backlog заметно
отличается от обычных проходов. Всего завершённые GC удалили 2809 announce
и 3978 пакетов; отличие от транзакционных сумм связано с границами отчётов.

`page_time_ms` включает ожидание worker и получение результата actor, поэтому
чистое SQL-время не может объяснить весь этот показатель. Даже предусмотренная
пауза примерно 3× время страницы сама по себе не объясняет всё время вне страниц:
существенны также условия допуска следующей страницы.

### Основной трафик и память

По actor: 737 пакетов / 2596 операций, **3,52 операции на пакет**,
119 одиночных пакетов (16,1%), 2414 обходов неизвестных путей. Среднее ожидание
допуска 5,36 мс, максимум 490,58 мс. Commit `apply` в среднем 112,13 мс,
максимум 515,97 мс; SQL до commit в среднем 0,117 мс.

Максимум очереди 23 из 64, учитываемый объём 26634 байта; максимум буфера записей
10990 байт. Зарегистрированных ошибок операций/commit, ERROR и сообщений
о переполнении/отбрасывании не обнаружено.

RSS на 5/10/15/20/25-й минутах: 21336 / 21752 / 23044 / 23532 / 23492 КиБ.
Последний RSS 22,94 МиБ, зафиксированный пик 24,01 МиБ. Последние два замера
почти одинаковы; вывод о долгосрочных утечках по этому запуску не делается.

### Следующий эксперимент

Сначала увеличить страницу GC с 16 до **32 ключей**, сохранив окно 2000 мс,
checkpoint, защиту пакетов, условия запуска и коэффициент паузы. Это небольшой
фиксированный рост временного списка ключей (до 512 байт для destination либо
1024 байт для packet hash, без учёта заголовка Vec), без накопления таблиц целиком.
Цель — объединить больше удалений в один commit и сократить число отдельных
запусков страниц. Проверять число записывающих транзакций на удалённую строку,
максимум времени страницы, очередь и полную длительность GC.

После отдельного измерения размера страницы рассматривать планирование:
ограниченный доступ GC к worker между пакетами вместо ожидания полного простоя.
Это меняет компромисс между длительностью очистки и задержкой входящего пакета;
смешивать его с изменением размера страницы в одном эксперименте не следует.

Рабочий код по результатам анализа `logfile.gc` не менялся; логи в git не добавлены.


### Реализован эксперимент: GC по 32 ключа

Размер страницы фонового GC увеличен с 16 до 32 ключей для очистки announce
и удаления пакетов. Критерии удаления, защита packet_keep/packet_refs,
коэффициент паузы 3× и условия запуска страницы сохранены. Одновременно
выполняется не более одной фоновой страницы; окно накопления и checkpoint
не изменены. Для сравнения с `logfile.gc` использовать окно 2000 мс.

Следующий лог должен показать изменение `main_write_transactions` относительно
`affected_main_rows`, количества `pages` и длительности GC. Также сравнить
максимум выполнения страницы и ожидание допуска: более крупная страница может
занимать worker дольше, хотя стоимость SQL в предыдущем тесте была мала.


## Результат logfile.gc_2: страница 32 ключа

Лог от 15 сентября 2026: 12:29:29–13:09:00 UTC, **39 минут 31 секунда**.
Сравнивается с `logfile.gc` (16 ключей, 29 минут 11 секунд). Максимальный
`items` GC-транзакции в новом логе равен 32, что подтверждает размер страницы.
В конце есть сигнал остановки, но нет финальных сводок actor/worker;
границы транзакционных и worker-сводок различаются.

### Нагрузка различается

По сводкам actor поток накопленных операций announce вырос с 88,97 до
134,90 в минуту (+52%). Пакетов записи стало 31,14 в минуту против 25,26;
средний пакет вырос с 3,52 до 4,33 операции. Эти изменения не позволяют
приписать разницу задержек только странице GC.

| Метрика | 16 ключей | 32 ключа |
| --- | ---: | ---: |
| Среднее ожидание допуска | 5,36 мс | 8,16 мс |
| Максимальное ожидание допуска | 490,58 мс | 864,91 мс |
| Средний commit `apply` | 112,13 мс | 119,17 мс |
| Максимум очереди | 23 | 30 из 64 |
| Максимум буфера записей | 10990 байт | 13813 байт |
| Медиана завершённого GC | 7,98 с | 11,92 с |
| Самый долгий завершённый GC | 566,13 с | 299,91 с |

Ошибок операций/commit, ERROR и сообщений о переполнении/отбрасывании не найдено.
Снижение максимальной длительности GC не доказывает ускорение: в самом долгом
новом проходе удалено 1547 announce и 1743 пакета против 2718 и 3075 в старом.

### Объединение удалений

| Операция и показатель | 16 ключей | 32 ключа |
| --- | ---: | ---: |
| Очистка announce: затронутых строк / записывающих транзакций | 2809 / 273 | 1547 / 69 |
| Очистка announce: строк на записывающую транзакцию | 10,29 | 22,42 |
| Удаление пакетов: затронутых строк / записывающих транзакций | 3969 / 763 | 3134 / 836 |
| Удаление пакетов: строк на записывающую транзакцию | 5,20 | 3,75 |

Для announce объединение стало плотнее. Для пакетов результат обратный,
но изменилась плотность удаляемых данных: раньше затронуто 3969 строк при
просмотре 27237 ключей (14,6%), теперь 3134 при 74976 (4,2%). Удвоенная
страница не гарантирует больше удалений, если подходящие строки редки.

Средний SQL до commit остался мал: 0,054 мс для очистки announce и 0,058 мс
для удаления пакетов; максимумы 0,227 и 0,168 мс. Максимум выполнения всей
страницы удаления пакетов 442,92 мс против 424,82 мс; очистки announce
275,23 мс против 350,53 мс. Лог не показывает существенного роста стоимости
самого SQL от перехода к 32 ключам.

### Ожидание GC остаётся главным ограничением

Завершены 29 проходов, обработано 4855 страниц. Суммарная длительность проходов
1038,108 с, время заданий страниц 65,086 с: **93,7% времени прошло вне заданий**.
Самый долгий проход: 299,914 с, 160 страниц, задания страниц 19,194 с.
Поздние проходы занимали 89–99 секунд при обработке страниц около 6,5–6,7 секунды.
Это подтверждает необходимость анализа планирования; изменение размера страницы
само по себе ожидание накопленных записей и входного трафика не устранило.

### Память и следующий шаг

RSS на 5/10/15/20/25/30/35-й минутах, КиБ:
21476 / 22200 / 23952 / 24492 / 26048 / 25656 / 26132.
Последний RSS 25,52 МиБ, зафиксированный пиковый RSS 28,40 МиБ. Первый замер
близок к предыдущему тесту, но новый запуск длиннее и входящий поток выше;
дальнейший рост RSS нельзя отнести только к странице GC или объявить утечкой.

Страницу 32 ключа оставить как текущий ограниченный вариант, без утверждения
об общем ускорении GC. Следующий отдельный шаг — контролируемый доступ одной
страницы GC между пакетами записей с сохранением защиты накопленных данных,
приоритета управляющих сообщений и ограничения частоты фоновой работы.
Нельзя просто запускать удаление при любых незакоммиченных announce:
сначала требуется проверить зависимости packet blob, pins и метаданных.
Окно 2000 мс и checkpoint при следующем эксперименте сохранить.

Рабочий код по результатам этого анализа не менялся. Логи в git не включены.


## Уровни диагностического логирования SQLite

Сводки транзакций и времени операций, admission/batch, настройка batching,
завершение sweep/GC/maintenance и сообщения о задержках переведены на DEBUG.
Существующие WARN о переполнении, сбоях обслуживания и некорректных действиях,
а также ERROR хранилища сохранены. RSS процесса остаётся INFO.

Для следующего замера включить:

```yaml
logging:
  filter: "info,rns_transport::actor::sqlite=debug,rns_transport::storage::metrics=debug"
  rss_interval: 300
```

Это включает агрегированные сводки и диагностику actor. При необходимости
добавить `rns_transport::storage::worker=debug` для сообщений начала, завершения
и задержки каждой операции worker. Старые разделы с анализом WARN описывают
уровни, действовавшие во время соответствующего теста.

## GC между пакетами записей

Следующий шаг реализован: успешный commit announce-пакета даёт возможность
выполнить одну страницу уже начатого GC перед подготовкой следующего обычного
входящего сообщения. Возможности не накапливаются. Если GC готов к запуску,
естественный flush выполняется отдельным заданием, чтобы следующий announce
сразу после commit не создал новый незаписанный пакет.

Условия и ограничения:

- Срок flush не сокращается ради GC. Настроенное окно batching сохраняется.
- Перед запуском нет накопленных записей, активного задания хранилища или
  отложенной инвалидации кеша. После ошибки commit эта возможность не возникает.
- Ожидающие RPC, чтение кешированного пакета и приоритетные сообщения блокируют
  запуск. Входные очереди и каналы также должны быть пустыми: ещё не
  классифицированное сообщение может оказаться приоритетным.
- Уже подготовленная очередь SQLite может содержать обычные входящие сообщения.
  Именно это отличает новую точку запуска от прежнего ожидания полного простоя.
- Пока эта страница выполняется, подготовка следующего сообщения хранилища
  приостановлена. Actor продолжает принимать сообщения и обслуживать команды,
  не требующие хранилища. Новое приоритетное обращение к хранилищу может ждать
  уже запущенную страницу; ещё одна страница перед ним не запускается.
- Сохранены страница **32 ключа**, пауза **3 × время предыдущего задания GC**,
  защита packet/pin/announce и завершение без дренирования всего GC при shutdown.
- Новых буферов данных нет: добавлены флаги состояния и интервальный счётчик.
  Лимиты очередей, пакета записей и кеша не увеличены.

Это дополнительная возможность продвижения GC, а не гарантия времени его
завершения: постоянно заполненные входные каналы или приоритетный трафик всё
ещё могут откладывать запуск.

В DEBUG-сводку `SQLite admission summary` добавлен `gc_between_batches` —
число запусков страниц через новую точку за интервал. Это число запусков,
не завершённых страниц и не проходов GC; обычные запуски в простое сюда не входят.

Добавлены тесты порядка `commit → GC → чтение следующего announce` с проверкой
сохранности обеих записей и допуска GC при обычной очереди, но не при RPC,
приоритетном или неклассифицированном входе и не до окончания паузы GC.

### Следующий нагрузочный замер

Сохранить `storage.announce_batch_delay_ms: 2000`, текущий checkpoint и тот же
DEBUG-filter SQLite. Сравнить при близкой интенсивности входящего потока:

- `gc_between_batches`, длительность проходов и `page_time_ms`;
- задержку admission и максимальное заполнение очереди;
- число batch/commit и записей на пакет;
- RSS при прежнем интервале 300 секунд.

Ускорение на одноплатнике ещё требует нагрузочного замера; локальные тесты
проверяют порядок и корректность обработки.

Проверки реализации: все 23 теста `actor::sqlite::tests` прошли последовательно;
отдельный запуск тестов `gc_` — 6/6. Прошли `cargo check --workspace` с
`rns-tools/api,rns-tools/sqlite-bundled` и проверка `rns-transport` без default
features. Тестовые логи в коммит не включены.
